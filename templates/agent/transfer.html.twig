{% extends 'base.html.twig' %}

{% block title %}Locality {% endblock %}

{% block body %}
    <div class="text-center mb-4 form-transfer">
    <h2>Transfer</h2>
     {{ form_start(formTransfer) }}
    {{ form_widget(formTransfer) }}
    <button class="btn btn-warning mt-4">{{ button_label|default('Transfer') }}</button>
    {{ form_end(formTransfer) }}

<a href="{{ path('agent_index') }}" class="btn btn-info mt-4">back to list</a>
    </div>
{% endblock %}